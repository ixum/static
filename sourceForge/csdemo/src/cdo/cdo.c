#include <stdio.h>
#include "cdo.h"

void cdo_init()
{
    int flag = 1;
    if (flag)
    {
        printf("flag = %d", flag);
    }
    else
    {
        printf("flag = %d", flag);
    } 
	printf("cdo module init ... \n");
}
