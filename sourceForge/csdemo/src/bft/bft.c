#include <stdio.h>
#include "bft.h"

void bft_init()
{
    int flag = 0;
    if (flag)
    {
        printf("flag = %d", flag);
    }
    else
    {
        printf("flag = %d", flag);
    } 
	printf("bft module init ... \n");
}
